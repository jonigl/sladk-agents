# In your app mgmt page: Go to Basic Information -> App-Level Tokens -> Generate Token and Scopes: connections:write
SLACK_APP_TOKEN=YOUR_SLACK_APP_TOKEN
# In your app mgmt page: Go to OAuth & Permissions -> Bot User OAuth Token
SLACK_BOT_TOKEN=YOUR_SLACK_BOT_TOKEN
# SLACK_API_URL=YOUR_SLACK_API_URL
# Timeout for Slack client requests in seconds (default: 30)
SLACK_CLIENT_TIMEOUT=45

# Your Google API key for accessing Google services
GOOGLE_API_KEY=YOUR_GOOGLE_API_KEY
# Set to TRUE to enable Google Vertex AI integration
GOOGLE_GENAI_USE_VERTEXAI=FALSE
# Specify the model to use for the AI agent e.g., gemini-2.5-flash for most use cases or gemini-3-pro-preview for state-of-the-art reasoning-
AGENT_MODEL=YOUR_AGENT_MODEL
# App name must be a valid identifier consisting of letters, digits, and underscores e.g., Sladk_App
AGENT_APP_NAME=YOUR_APP_NAME
# Agent name must be a valid identifier. It should start with a letter (a-z, A-Z) or an underscore (_),
# and can only contain letters, digits (0-9), and underscores e.g., Sladk_AI_Agent
AGENT_NAME=YOUR_AGENT_NAME

# Optional: override the default system instruction used by the LLM. Use a single line or triple-quoted
# string style in your environment management if supported. If unset, a reasonable default is used.
# Example (single-line): DEFAULT_SYSTEM_INSTRUCTION="You're an assistant in a Slack workspace..."
DEFAULT_SYSTEM_INSTRUCTION=YOUR_DEFAULT_SYSTEM_INSTRUCTION
